// Fast I/O Template
#include <bits/stdc++.h>
#define get getchar_unlocked
#define put putchar_unlocked

using namespace std;

long long int read()
{
    long long int n=0;
    char c=get();
    while(c<48||c>57) c=get();
    while(c>=48&&c<=57)
    {
        n=(n<<3)+(n<<1)+(c-48);
        c=get();
    }
    return n;
}
void write(int n)
{
    char a[12];
    int i=0;
    do
    {
        a[i++]=n%10+48;
        n/=10;
    } while(n>0);
    while(i--) put(a[i]);
    put(' ');
}
int main()
{
    int t=read();
    while(t--)
    {
        int n=read(),a[n];
        for(int i=0; i<n; i++)
            a[i] = read();

            
    }
}
